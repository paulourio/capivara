#ifndef VIEW_H
#define VIEW_H

/* view.c */

/* Note: the return pointer must be released from memory. */
extern char *create_index_view(void);

/* injetor.c */
extern int injetar_arquivo(char *buff, char *arq);


#endif

